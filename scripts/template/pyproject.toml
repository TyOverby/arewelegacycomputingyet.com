[project]
name = "template"
version = "0.1.0"
description = "Templating script for arewelegacycomputingyet.com"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.12",
]

[tool.uv]
dev-dependencies = []
